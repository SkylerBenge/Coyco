<div class="mt-20 mb-20 max-w-screen-xl mx-auto">
  {% include 'about-nav' %}
  <h1 class="heading">{{ page.title }}</h1>

  <div class="grid portfolio-grid">{{ page.content }}</div>
</div>

{% include 'mosaic' %}